spring:
  application:
    name: ecommerce-test
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: false

server:
  port: 0  # 랜덤 포트

logging:
  level:
    com.phm.ecommerce: DEBUG
